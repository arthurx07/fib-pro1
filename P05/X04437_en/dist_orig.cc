// Write a function: double dist_or(double x, double y); which returns the distance to the origen from the pint (x,y)
// Input: Two real numbers;
// Observation:  You only need to submit the required procedure; your main program will be ignored. 

#include <cmath>


double dist_or(double x,double y) {
  return sqrt(x*x+y*y); // distance_orig = square root of: x^2 + y^2
}
